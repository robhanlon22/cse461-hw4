<ul>
  <% if latest_photos.empty? -%>
	  <li><a href="#">No photos to show</a></li>
	<% else -%>
	  <% for photo in latest_photos -%>
	    <li>
	      <%= link_to truncate(photo.uid, :length => 20), :action => :view, :uuid => photo.uid %>
	    </li>
	  <% end -%>
	<% end -%>
</ul>
